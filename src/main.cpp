#include <iostream>

#include "solution.hpp"

int main() {
    std::cout << "Current problem: 在排序数组中查找元素的第一个和最后一个位置" << '\n';
    std::cout << "Slug: find-first-and-last-position-of-element-in-sorted-array" << '\n';
    std::cout << "Edit src/solution.hpp and src/main.cpp for local testing." << '\n';
    return 0;
}
